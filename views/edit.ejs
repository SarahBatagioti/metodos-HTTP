<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Editar Cliente</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>Editar Cliente</h1>
  <form action="/clientes/editar/<%= cliente.id %>" method="POST">
    <label for="nome">Nome:</label>
    <input type="text" name="nome" value="<%= cliente.nome %>" required><br><br>
    
    <label for="email">Email:</label>
    <input type="email" name="email" value="<%= cliente.email %>" required><br><br>
    
    <label for="status">Ativo:</label>
    <input type="checkbox" name="status" <%= cliente.status ? 'checked' : '' %>><br><br>
    
    <button type="submit">Atualizar Cliente</button>
  </form>
  <a href="/">Voltar para a lista</a>
</body>
</html>
